# Bearer token the CRE workflow uses to authenticate with this API.
# Must match the KYC_API_KEY secret set in CRE Vault DON.
MOCK_API_BEARER_TOKEN=mock-bearer-token-123

# Port — Railway sets this automatically. Override locally if needed.
PORT=3001

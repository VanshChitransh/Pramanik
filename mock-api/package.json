{
  "name": "pramanik-mock-api",
  "version": "1.0.0",
  "description": "Mock KYC API server simulating Jumio/Onfido/Chainalysis",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun"
  },
  "dependencies": {
    "@types/cors": "^2.8.19",
    "cors": "^2.8.6",
    "express": "^4.19.0",
    "express-rate-limit": "^8.3.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "typescript": "^5.5.0"
  }
}
